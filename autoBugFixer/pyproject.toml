[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "autobugfixer"
version = "0.1.0"
description = "自动化 Bug 修复系统：缺陷平台拉取 -> 预处理 -> AI 修复 -> 部署验证 -> 经验沉淀"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2",
    "pydantic-settings>=2",
    "sqlalchemy>=2",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "langchain>=1",
    "langchain-anthropic>=0.3",
    "cryptography>=42",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "httpx>=0.27",
]

[project.scripts]
autobugfixer-api = "autobugfixer.api.app:main"
autobugfixer-import = "autobugfixer.cli.import_cli:main"
autobugfixer-scheduler = "autobugfixer.cli.scheduler_cli:main"
autobugfixer-export = "autobugfixer.cli.export_cli:main"
autobugfixer-repo = "autobugfixer.cli.repo_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"autobugfixer.common.prompts" = ["templates/*.md", "rubrics/*.md"]
"autobugfixer.api.web" = ["static/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
