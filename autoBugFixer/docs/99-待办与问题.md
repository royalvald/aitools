# 待办与问题跟踪

> 记录当前未闭环的问题与待办，解决一项勾掉一项并注明日期。
> 边界决策类问题解决后同步更新 `00-边界定义.md`。

## 阻塞中（边界确认收尾）

- [ ] **Bugzilla 版本及 REST API 可用性确认**（2026-07-26 记录）
  - 背景：Bugzilla 5.0+ 自带 REST API。第一阶段 bug 来源定为导出文件导入，若确认 API 可用，后续可升级为 API 直连（读取 + 评论回写）。
  - 动作：确认测试/生产 Bugzilla 部署的版本号，以及是否可申请 API token。

- [ ] **测试步骤真实样例 2-3 个（脱敏）**（2026-07-26 记录）
  - 背景：分析 prompt 和验证计划结构需要按真实数据格式落地，是边界阶段最后一个阻塞项。
  - 动作：从 Bugzilla 找 2-3 个典型接口类 bug 单，脱敏后提供（标题、描述、复现步骤、预期/实际结果）。

## 后续迭代候选（已明确排期外，防止遗忘）

- [ ] 通知渠道接入（企微/钉钉 webhook 提醒）——第一阶段定为不做，纯本地审阅。
- [ ] Bugzilla API 直连（依赖上方版本确认）。
- [ ] memory 向量检索（第一阶段只做模块+关键词匹配）。
- [ ] 运行指标看板（全自动率、一次修复成功率等）。
- [ ] 多项目/多仓库接入（第一阶段只接 1 个 Go 项目）。
- [ ] UI 自动化验证（Playwright 页面操作类）。
